[build]
builder = "nixpacks"

[deploy]
startCommand = "(npm run db:init || echo 'pgvector init skipped') && npm run db:push && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 180
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
